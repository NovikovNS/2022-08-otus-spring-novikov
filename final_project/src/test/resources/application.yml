spring:
  datasource:
    url: jdbc:h2:mem:testdb
  sql:
    init:
      mode: always
      data-locations: test_data.sql
      encoding: UTF-8
  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none
    show-sql: true