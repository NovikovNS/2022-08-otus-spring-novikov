spring:
  datasource:
    url: jdbc:h2:mem:testdb
  liquibase:
    enabled: true
    change-log: /changelog/testChangelog.yaml
  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
    show-sql: true